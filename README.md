## INSTALL 

RUN: `yarn` 

## CONFIGURATION 

Add this file:
.env 
`BASE_URL = https://echo-serv.tbxnet.com/`

Pod Install: 
RUN: `cd ios && pod install && cd ..`

## RUN 

RUN: `react-native run-ios`